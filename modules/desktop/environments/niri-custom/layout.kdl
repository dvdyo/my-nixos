layout {
    // Set gaps around windows in logical pixels.
    gaps 16
    background-color "transparent"
    // When to center a column when changing focus.
    center-focused-column "never"

    preset-column-widths {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
    }

    // Terminals and new windows start at 1/2 width
    default-column-width { proportion 0.5; }

    focus-ring {
        width 0.5 
        active-color   "#cba6f7" // Catppuccin Mauve
        inactive-color "#585b70" // Surface 2
        urgent-color   "#f38ba8" // Red
    }

    border {
        off
    }

    shadow {
        on
        softness 30
        spread 5
        offset x=0 y=5
        color "#00000040"
    }
}

// Window Rules for Aesthetics
window-rule {
    geometry-corner-radius 12
    clip-to-geometry true
}

window-rule {
    match is-active=false
    opacity 0.95
}

// Ensure the wallpaper stays static behind workspace transitions
layer-rule {
    match namespace="awww-daemon"
    place-within-backdrop true
}

// Disable rounding for apps that look bad with it (optional, but good practice)
// window-rule {
//     match app-id="firefox"
//     geometry-corner-radius 0
// }
